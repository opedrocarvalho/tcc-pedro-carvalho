destinosbrasil:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost      
      port: 5432
      user: pipeline
      password: pipeline
      dbname: destinosbrasil
      schema: staging
      threads: 4
    docker:
      type: postgres
      host: postgres       
      port: 5432
      user: pipeline
      password: pipeline
      dbname: destinosbrasil
      schema: staging
      threads: 4
